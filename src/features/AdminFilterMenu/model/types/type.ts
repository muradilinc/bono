export interface DataFilterCard {
  title: string;
}

export interface DataMenuCard {
  id: number;
  category: string;
  img: string;
  title: string;
  text: string;
  gram: number;
  price: number;
}
